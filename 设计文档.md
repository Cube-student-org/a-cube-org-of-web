# 魔方比赛成绩管理系统 · 设计文档

| 项 | 内容 |
|---|---|
| 文档版本 | v1.0（2026-09-13） |
| 对应代码版本 | `main` @ `bc7d16e` |
| 前端线上地址 | https://cube-student-org.github.io/a-cube-org-of-web/ |
| 后端线上地址 | https://a-cube-org-of-web.pages.dev |
| 代码仓库 | `Cube-student-org/a-cube-org-of-web` |
| 技术栈 | 原生 HTML/CSS/JS（无框架、无构建） + Cloudflare Pages Functions + Supabase（PostgreSQL + GoTrue） |

---

## 1. 这个系统要干什么

### 1.1 业务目标

一句话：**给魔方比赛的成绩做一套"能多人协作、但只有可信的人才能改数"的记录与查询系统。**

具体要满足的日常动作：

- 编辑员维护基础数据：比赛、项目（含项目层级）、选手；
- 编辑员把某场比赛和某些项目关联起来（`competition_events`），再逐条录入选手成绩；
- 成绩不能录了就算数——要经**审核**才生效，对外查询只看到已通过的成绩；
- 管理员能建账号、发角色，控制谁能录入、谁能审核；
- 任何人（含未登录访客）都能查已通过的成绩与统计，不需要注册。

### 1.2 三条硬约束（决定了后面所有设计）

本项目绝大多数"看起来复杂"的机制，都不是为了复杂而复杂，而是被下面三条约束逼出来的。后续每一章都会标明它在回应哪一条。

| 编号 | 约束 | 来源 | 直接后果 |
|---|---|---|---|
| **C1** | 浏览器不接触数据库 | 项目方明确要求：**"我们现在是通过云函数来访问数据库，也就意味着我们这里不对外说要输入数据库"** | 前端不得出现任何 URL / Key / "连接数据库"界面；一切数据与认证走自建云函数 |
| **C2** | 权限必须在数据层强制 | 前端代码可被任意篡改，前端的角色判断只是"界面礼貌"，不是安全边界 | 真正的权限写在 PostgreSQL 的 RLS 策略与触发器里；云函数只做"提前报错" |
| **C3** | 国内网络必须能直连 | `*.workers.dev` 在国内被 **DNS 污染 + SNI 阻断**双重封锁 | 后端最终落在 `*.pages.dev`（Cloudflare Pages 高级模式） |

三条约束的优先级是 C1 > C2 > C3：C3 是可以换域名的部署问题，C2 是安全底线，C1 是架构形态。

### 1.3 非目标（明确不做的）

写清楚"不做什么"同样重要，避免后续把精力投错方向：

- **不做公开自助注册**。账号一律由管理员创建（`/admin-create-user`），因为这是校/社团内部的成绩系统，不是开放平台。
- **不做花哨的统计预测**。曾有一版 Gamma MLE 分布拟合与预测，已整体移除，`js/stats.js` 现在只剩一行说明。理由是这类功能"名义上宏大"，但对"查成绩"这个真实需求没有增益。
- **不做计时器硬件接入 / 实时对战 / 直播**。
- **不做离线优先**。没有 Service Worker，没有本地数据库。

---

## 2. 总体架构

### 2.1 三层结构

```
┌──────────────────────────────────────────────────────────────┐
│  ① 浏览器（静态站）                                            │
│     GitHub Pages: cube-student-org.github.io/a-cube-org-of-web │
│                                                              │
│     index.html + css/style.css + js/*.js  （原生，无构建）      │
│     持有：access_token / refresh_token（localStorage）         │
│     不持有：Supabase URL、任何 Supabase Key                    │
└───────────────────────────┬──────────────────────────────────┘
                            │  HTTPS + Bearer <access_token>
                            │  （100% 走下面这个域名的 /api/*）
                            ▼
┌──────────────────────────────────────────────────────────────┐
│  ② 云函数（唯一后端入口）                                      │
│     Cloudflare Pages 高级模式：cf-pages/dist/_worker.js        │
│     https://a-cube-org-of-web.pages.dev                       │
│                                                              │
│     职责：路由、CORS、输入校验、认证代理、数据代理              │
│     持有：SUPABASE_URL / ANON_KEY / SERVICE_ROLE_KEY（环境变量）│
│     原则：数据面一律用【调用者的 JWT】转发，不越权、不提权       │
│           SERVICE_ROLE_KEY 只用于"建号"这一个天然高权限动作     │
└───────────────────────────┬──────────────────────────────────┘
                            │  HTTPS + Bearer <调用者的 JWT>
                            ▼
┌──────────────────────────────────────────────────────────────┐
│  ③ Supabase                                                    │
│     ・PostgREST（/rest/v1/*）  ← 数据面                        │
│     ・GoTrue   （/auth/v1/*）  ← 认证面                        │
│     ・PostgreSQL：6 张业务表 + RLS 策略 + 5 个安全触发器        │
│       ★ 权限的最终裁决者就在这里，前两层都绕不过它              │
└──────────────────────────────────────────────────────────────┘
```

### 2.2 三条典型链路

**A. 登录**（回应 C1：浏览器不该知道 Supabase 存在）

```
浏览器  POST /api/auth/login  {code, password}
   ↓
云函数  code → 拼成 <code>@cube.local → 转发 POST /auth/v1/token?grant_type=password
   ↓
Supabase 校验通过，返回 {access_token, refresh_token, user}
   ↓
浏览器  写入 localStorage（wb_access / wb_refresh / wb_user）
```

关键点：浏览器从头到尾只知道 `a-cube-org-of-web.pages.dev` 这一个域名。

**B. 读数据**（匿名可读，回应 C2）

```
浏览器  GET /api/competitions?select=*&order=competition_number     （不带 Authorization）
   ↓
云函数  不带 token 也放行（GET）；以 anon key 转发
   ↓
Postgres  RLS「public read competitions」USING (true) → 返回数据
```

**C. 提交成绩**（回应 C2：状态由服务端定，前端传什么都不算）

```
浏览器  POST /submit-attempt  {competition_event_id, participant_id, solve_time, ...}
   ↓
云函数  ① verifyCallerJwt   → 用调用者 token 问 /auth/v1/user，拿到 uid
        ② fetchUserRole     → 以调用者身份读 profiles.role
        ③ 角色白名单校验    → 非 editor/reviewer/admin → 403
        ④ 字段白名单过滤    → SUBMIT_ALLOWED_FIELDS，前端多传的字段全丢
        ⑤ 哨兵值翻译        → solve_time=-1→DNF，-2→DNS
        ⑥ status 由角色决定 → admin 直接 approved，其余强制 pending
        ⑦ 以调用者 JWT 插入
   ↓
Postgres  RLS「attempts insert」再次校验 → 双保险
```

注意 ⑥：**"我是不是管理员"这个判断在云函数里做了一次，在 RLS 里又做了一次**。这不是冗余，是刻意的——云函数那次是为了给出人能看懂的错误信息，RLS 那次才是真正的锁（见 §4）。

### 2.3 约束 → 机制对照表

| 约束 | 为它设计的机制 | 在哪实现 |
|---|---|---|
| C1 浏览器不碰数据库 | `/api/auth/*` 认证代理 | `worker.js` `authProxy()` |
| C1 | `/api/<table>` 通用数据代理（对齐 PostgREST 语义） | `worker.js` `apiProxy()` |
| C1 | 前端 `db()` 链式构建器，语法对齐 supabase-js 以便平滑迁移 | `js/api.js` |
| C1 | 移除全部"连接数据库"UI 与 supabase-js 依赖 | `index.html`、`js/supabase-client.js` |
| C2 权限在数据层 | 6 张表的 RLS 策略矩阵 | `schema.sql` §5 |
| C2 | 5 个触发器补策略覆盖不到的缝 | `schema.sql` §5b |
| C2 | 云函数白名单 + 服务端定状态 | `worker.js` 各端点 |
| C2 | `SERVICE_ROLE_KEY` 只用于建号，绝不用于数据面 | `worker.js` `supabaseAdminRest()` |
| C3 国内可直连 | 后端迁到 Pages 高级模式（Worker 代码零改动） | `cf-pages/dist/_worker.js` |
| C3 | CORS 预检 204 不得带 body（否则整个前后端通信全挂） | `worker.js` 主入口 |
| C3 | 连接失败时把目标地址一并抛出，便于一眼定位 | `js/config.js` `callWorker()` |

### 2.4 为什么后端跑在 Pages 而不是 Workers

这不是偏好问题，是**实测出来的**（2026-09-12）：

| 层面 | 现象 | 结论 |
|---|---|---|
| DNS | `…workers.dev` 解析到 `157.240.1.9` / Facebook IPv6 段 | 域名被投毒 |
| TCP | 指定真实 IP 后握手成功（0.21s） | 网络层其实可达 |
| TLS | 同一连接上 `failed to receive handshake` | **SNI 被识别后掐断** |

拦在 SNI 层，意味着改 hosts、换 DNS 全部无效，只能换域名。实测 `*.pages.dev` 在同一网络下 HTTP 200、TLS ~0.7s 完全可用。

迁移成本几乎为零的原因：Pages 的**高级模式**接受与 Worker 完全相同的 Module 语法（`export default { fetch(request, env) }`），本项目 `cf-workers/worker.js` 正好就是这套写法，复制过去即可。

> **单文件约束**：`cf-workers/worker.js` 是刻意写成**单文件、无 import** 的，因为要能直接粘进 Cloudflare 网页控制台的在线编辑器。`cf-workers/src/` 下的拆分版本是历史遗留，不是部署产物。

---

## 3. 身份与角色

### 3.1 用户 ID（回应 C1）

用户看到的是**用户ID**（如 `u00000001`），不是邮箱。但 Supabase Auth 只认邮箱，所以云函数做一层确定性映射：

```js
function loginCodeToEmail(code) {
  return code + "@cube.local";     // u00000001 → u00000001@cube.local
}
```

这个映射必须在**建号**和**登录**两处完全一致，否则建出来的号登不进去。两处都调用同一个函数。

**大小写兜底**：用户 ID 长得像编号，人看到 `u00000001` 的第一反应是打全大写 `U00000001`，而邮箱在库里可能是小写。所以登录逻辑是：

1. 先按输入原样试一次（保住历史上用大写建的账号）；
2. **仅当**返回 401 且输入含大写时，再用全小写重试一次。

成功路径的行为完全不变，只是多一层容错。这一点很重要——上线前它导致的报错是 `Invalid login credentials`，看起来像密码错，实际是大小写。

`user_code` 有两个来源：

- 管理员建号时**由管理员直接指定**（`/admin-create-user` 把它写成 `user_code`，与登录 ID 一致）；
- 自助注册（部署时的首位管理员）走触发器默认值 `'U' || lpad(nextval('user_code_seq'), 6, '0')`。

### 3.2 五级角色

角色是**线性等级**，不是权限位的集合。等级比较是前端显隐、云函数校验、RLS 判断共同的基础。

| 角色 | rank | 可维护基础数据 | 可录入成绩 | 录入后的状态 | 可审核 | 可管用户 |
|---|:---:|:---:|:---:|---|:---:|:---:|
| `anon` 匿名 | 0 | — | — | — | — | — |
| `user` 普通用户 | 1 | — | — | — | — | — |
| `editor` 编辑员 | 2 | ✅ | ✅ | **pending 待审核** | — | — |
| `reviewer` 审核员 | 3 | ✅ | ✅ | **pending 待审核** | ✅ | — |
| `admin` 管理员 | 4 | ✅ | ✅ | **approved 直接生效** | ✅ | ✅ |

两个反直觉但**刻意如此**的设计：

1. **审核员没有直接录入权**。审核员自己录的成绩也必须走待审。"能审自己"是审计上的禁忌，与其靠"防自审"这种补丁去堵，不如从源头不给这个能力。
2. **管理员不可被他人创建或提权**。`CREATE_ALLOWED_ROLES` 和 `ASSIGNABLE_ROLES` 都只有 `['user','editor','reviewer']`；RLS 里 `profiles admin update` 写明 `USING (is_admin() AND role <> 'admin')`。管理员的产生只有一条路：部署时在 Supabase 后台手动建号 + 手动 `UPDATE profiles SET role='admin'`（见 §10.3）。

### 3.3 会话

Session 层（`js/session.js`）替代了 supabase-js 的 auth 模块，只有三个 key：

| localStorage key | 内容 |
|---|---|
| `wb_access` | access token（短期） |
| `wb_refresh` | refresh token（长期） |
| `wb_user` | 用户对象 |

- `authToken()`（`js/config.js`）：本地有 access 就直接用，否则用 refresh 换新的（`ensureFresh()`）；刷新失败就清空会话，退化为匿名。
- 所有 `/api/*` 请求自动附带 `Authorization: Bearer <access>`。
- **账号信息完全信任数据库**：`refreshMyProfile()` 在窗口 `focus` 时同步一次，另有 5 分钟兜底轮询。角色被管理员改了，用户切回页面即生效，**不需要重新登录**。

---

## 4. 权限的落点：RLS 是唯一权威

这一章是本系统的核心。回应的是 C2。

### 4.1 三层防线，只有一层是真的

| 层 | 位置 | 作用 | 能被绕过吗 |
|---|---|---|---|
| ① 界面层 | `data-minrole` 元素显隐 | 不显示没权限的功能 | ✅ 改 DOM 即可 |
| ② 云函数层 | `verifyCallerJwt` + 角色白名单 | 把错误翻译成人话、提前拦 | ⚠️ 只在走这个端点时有效 |
| ③ **数据库层** | **RLS 策略 + 触发器** | **真正的锁** | ❌ 无 |

关键认识：**如果只有 ①②，这个系统等于没有权限系统**。别人直接 `curl` 你的 Supabase 端点、或者自己写个前端指向同一个云函数，① 完全无效；② 只要端点有任何一个疏漏（历史上就有过）就会被穿。所以权限必须落在 Postgres 里，因为那是**所有路径的必经之地**。

这也是为什么云函数里的每一次角色校验，在 RLS 里都有一个对应的策略——不是重复劳动，是"可读的错误信息"与"不可绕过的锁"各司其职。

### 4.2 RLS 策略矩阵

6 张表的策略如下（`schema.sql` §5）：

| 表 | SELECT | INSERT | UPDATE | DELETE |
|---|---|---|---|---|
| `competitions` | 所有人（含匿名） | 编辑员+ | 编辑员+ | 编辑员+ |
| `events` | 所有人 | 编辑员+ | 编辑员+ | 编辑员+ |
| `competition_events` | 所有人 | 编辑员+ | 编辑员+ | 编辑员+ |
| `participants` | 所有人 | 编辑员+ | 编辑员+ | 编辑员+ |
| `attempts` | 见下 | 见下 | 见下 | 见下 |
| `profiles` | 登录用户 | 触发器自动建 | 见下 | — |

`attempts` 是唯一有细粒度逻辑的表：

```sql
-- 读：已通过的公开；待审/已驳回仅审核员以上 + 提交者本人可见
FOR SELECT USING (
    status = 'approved'
    OR is_reviewer_or_above()
    OR (auth.uid() IS NOT NULL AND submitted_by = auth.uid())
);

-- 写：管理员可直接录 approved；编辑员/审核员提交强制 pending；
--     submitted_by 强制等于调用者本人
FOR INSERT TO authenticated WITH CHECK (
    ((is_admin() AND status = 'approved')
    OR (is_editor_or_above() AND NOT is_admin() AND status = 'pending'))
    AND submitted_by = auth.uid()
);

-- 改：审核员以上可改任何；编辑员可修正已通过成绩但不能改审核状态
-- 删：审核员以上；编辑员只能删自己的待审提交
```

`profiles` 的三条策略值得单独看，因为管理员规则就在这里：

```sql
-- 管理员：只能改「身份比自己低」的行，且不能把谁变成管理员
USING      (is_admin() AND role <> 'admin')
WITH CHECK (is_admin() AND role <> 'admin')

-- 本人：可以改自己的昵称，但角色必须保持不变
--（唯一的例外 has_no_admin()：系统还没有管理员时允许自助提升，用于部署引导）
WITH CHECK (auth.uid() = id AND (role = get_my_role() OR has_no_admin()))
```

`has_no_admin()` 这个引导口子是刻意的：全新部署的库没有任何管理员，而"创建管理员"本身需要管理员权限——鸡生蛋问题。口子只在**一个管理员都没有**的时候打开，一旦有了管理员立即失效。部署完成后这个函数永远返回 false，等同于关闭。

### 4.3 触发器补的四条缝

RLS 策略表达力有限，有些约束它写不出来，这里用触发器兜底（`schema.sql` §5b）：

| 触发器 | 补的是什么缝 |
|---|---|
| `prevent_self_review` | RLS 无法表达"我改的这行的 `submitted_by` 不能是我"。当 `status` 发生变化时，若 `submitted_by = auth.uid()` 直接 `RAISE EXCEPTION '不能审核自己提交的成绩'` |
| `protect_review_columns` | RLS 是**行级**的，管不了"这一行里某几列谁能改"。编辑员可以 UPDATE 已通过的成绩（合法），但不能顺手改 `reviewed_by` / `reviewed_at`（会破坏审计） |
| `backfill_submitted_by` | INSERT 时若没带 `submitted_by` 就自动补 `auth.uid()`。堵的是"插进去了却查不回来"——插入没人拦，但查的时候 SELECT 策略要求 `submitted_by = auth.uid()` |
| `prevent_event_cycle` | `events.parent_event_id` 成环会让 `get_event_full_config()` 无限递归。触发器沿父链向上走最多 50 层，发现回到自身即拒绝 |

### 4.4 攻击测试验证

权限体系不是"看着对"就算数。本项目用 **PGlite**（WASM 版 PostgreSQL，内存运行，退出即销毁）在本地完整复现了 Supabase 环境——三角色（`anon`/`authenticated`/`service_role`）、真实 `SET ROLE` 切换 PostgreSQL 角色、`auth.uid()` 按 Supabase 语义实现为读取 `request.jwt.claim.sub`。

发现问题 → 逐项修复 → 全量复测，最终结果：

| 测试集 | 结果 |
|---|---|
| 部署核验（RLS 已启用、无 anon 写策略、SECURITY DEFINER 白名单、匿名写入被拒） | 4/4 |
| 第一层：RLS 越权矩阵（三角色 × 读/写/提权/伪造/注入） | 42/42 |
| 第二层：云函数逻辑 / 函数滥用 / DoS / 审计完整性 | 13/13 |
| 修复专项回归 | 8/8 |

真实修掉的高危问题有两个，值得记录：

1. **存储型 XSS**：`data-loader.js` 里 `sel.innerHTML += '<option>' + p.name + '</option>'` 直接拼选手名，而选手名是编辑员可写的 → 任何人打开下拉框都会执行注入的脚本。修法是引入 `escHtml()`，所有进 `innerHTML` 的动态数据一律先转义。
2. **审核员可自审**：云函数只校验了角色，没校验"这条成绩是不是别人提交的"。修法是云函数加 `submitted_by <> uid`，同时加数据库触发器作为最终防线——**同一个漏洞补两次，一次在能说话的地方，一次在不能绕过的地方**。

保留未修的两项，理由都记录在案：`role` 是 PostgreSQL 保留字做列名（真实 Supabase 的 PG15/16 可用，更名牵动三处代码、收益低）；`sub` 算法 threshold 无上界（属统计口径问题，非安全漏洞）。

---

## 5. 成绩审核流

### 5.1 状态机

```
                    编辑员/审核员提交            审核员点「通过」
   （不存在） ──────────────────────▶ pending ──────────────────▶ approved
                                          │                          ▲
                                          │ 审核员点「驳回」          │ 管理员直接录入
                                          ▼                          │
                                      rejected                  （跳过 pending）
```

三条状态规则：

- `approved`：对**所有人**可见（含匿名），参与统计；
- `pending`：仅审核员以上 + **提交者本人**可见（本人能看到自己刚交的还在等）；
- `rejected`：仅审核员以上 + 提交者本人可见。

存量数据在升级时默认 `approved`（`ALTER TABLE attempts ADD COLUMN status TEXT NOT NULL DEFAULT 'approved'`），保证老数据不会突然查不到。

### 5.2 状态由服务端决定，不由前端传

`addAttempt()` 前端确实会算 `isDnf` / `isDns` 并传上去，但**不传 `status`，也不传 `submitted_by`**。云函数有一份字段白名单：

```js
const SUBMIT_ALLOWED_FIELDS = [
  "competition_event_id", "participant_id", "attempt_number",
  "solve_time", "cube_type", "scramble", "move_count", "tps",
  "solve_steps", "step_comments", "is_dnf", "is_plus_two",
  "is_dns", "video_url", "notes",
];
```

白名单之外的字段（包括 `status`、`submitted_by`、`reviewed_by`）**被直接丢弃**，然后服务端自己补：

```js
record.submitted_by = uid;
record.status = role === "admin" ? "approved" : "pending";
```

即使有人在浏览器控制台里手动构造请求塞 `"status":"approved"`，也会被丢掉，然后被 RLS 再拦一次。

### 5.3 防自审与并发

**防自审**同样是两道：

- 云函数：`if (target.submitted_by === uid) throw httpError(403, "不能审核自己提交的成绩")`
- 数据库：`trg_prevent_self_review`

**并发处理**用乐观锁。两个审核员同时点"通过"和"驳回"，如果不处理，后写的会覆盖先写的。做法是把状态条件写进 PATCH 的筛选串：

```js
PATCH /rest/v1/attempts?id=eq.<id>&status=eq.pending   { status, reviewed_by, reviewed_at }
```

只有仍是 `pending` 的行才会被命中。若已被别人处理，影响行数为 0 → 云函数返回 **409「该成绩已被处理，请刷新后重试」**。这个"用影响行数判断并发冲突"的手法，在 `assignRole` 里也用于检测"目标用户在查询后被删除"。

### 5.4 成绩的三种特殊状态用哨兵值表达

用户输入框里填 `-1` / `-2` 表示 DNF / DNS，云函数在写库前翻译成布尔列：

| 输入 | `solve_time` | `is_dnf` | `is_dns` |
|---|---|---|---|
| `12.345` | `12.345` | false | false |
| `-1` | `null` | **true** | false |
| `-2` | `null` | false | **true** |

翻译后 `solve_time` 存 `null`，语义集中在布尔列上。这样统计、显示、排序都只需看布尔列，不用反复判断"-1 到底是不是特殊值"。其他负数（如 `-5`）直接 400 拒绝。

---

## 6. 数据模型

### 6.1 关系

```
competitions ──┐
               ├──< competition_events >── events ──┐
               │        (关联表)             │       │ 自引用
               │                            └───────┘ parent_event_id
               │
               └────────────┐
                            ▼
                        attempts ──── participants
                            │
                            └─ submitted_by / reviewed_by ──▶ auth.users ──▶ profiles
```

设计要点：

- **`competition_events` 是必需的中间层**，不是多余的。一场比赛的"三阶"和另一场比赛的"三阶"是两个不同的参赛单元（`event_number` 不同、参赛人不同、成绩不同）。成绩挂在 `competition_event_id` 而不是 `event_id` 上，就是这个原因。
- **`events` 自引用做层级**。顶层是 `3x3`（三阶），子项是 `3x3-ao5`、`3x3-oh` 等。层级不是为了显示好看，是为了**配置继承**（见 §7.5）。
- **`profiles.id` 外键指向 `auth.users.id`**，一张 1:1 扩展表。角色存在这里而不是塞进 JWT claim，是为了改动能立即生效——JWT 里的 claim 是签发时快照，改了角色要等 token 过期才生效。

### 6.2 表清单

**`competitions`** 比赛：`competition_number`、`name`、`competition_date`、`location`、`notes`

**`events`** 项目：`event_code`、`event_name`、`description`、`parent_event_id`、`is_sub_event`、**`event_config` JSONB**、**`algorithm_config` JSONB**、`sort_order`

**`competition_events`** 比赛×项目：`competition_id`、`event_id`、`event_number`

**`participants`** 选手：`name`、`wca_id`

**`attempts`** 成绩（核心表）：

| 分组 | 字段 |
|---|---|
| 归属 | `competition_event_id`、`participant_id`、`attempt_number` |
| 成绩 | `solve_time`、`is_dnf`、`is_plus_two`、`is_dns`、`cube_type` |
| 智能魔方 | `move_count`、`tps`、`solve_steps`、`step_comments`、`scramble` |
| 佐证 | `video_url`、`notes` |
| 审核 | `status`、`submitted_by`、`reviewed_by`、`reviewed_at` |

**`profiles`** 用户：`id`(=auth.users.id)、`user_code`、`username`、`role`、`created_at`、`updated_at`

### 6.3 已废弃的表

升级过程中删掉了 4 张表，都是"设计了但没接上"的：`event_algorithms`、`statistics_definitions`、`mle_predictions`、`participant_statistics`（Gamma MLE 预测那一套），另有 3 个视图 `competition_stats` / `participant_stats` / `event_config_view` 被删——它们不仅没用，还绕过 RLS 把 `pending` 成绩的计数泄露给匿名用户。清理脚本在 `db-cleanup.sql`。

---

## 7. 项目配置系统

这是本项目"看起来最宏大"的一部分。值得先说清楚它**为什么存在**。

### 7.1 为什么要有两套 JSONB 配置

魔方比赛的项目差异极大：

- 三阶速拧比的是 5 次平均、去掉最好和最差；
- 盲拧比的是 3 次取最好，观测时间算进总时长；
- FMC 比的是最少步数，越低越好；
- SUB-10 是统计"有多少次进 10 秒"，输出的是百分比不是秒数。

如果把这些差异都写成列，`events` 表会长出一堆互相排斥的字段；如果写成代码分支，每加一个玩法就要改前端。所以选择**把差异放进两条 JSONB**：

- **`event_config`** —— 这个项目"是什么"（是否支持智能魔方、记录哪些数据、用不用 WCA 规则、显示在不在排行榜）；
- **`algorithm_config`** —— 这个项目的成绩"怎么算"（算法类型、窗口大小、去头尾数量、方向）。

代价是数据库不校验 JSON 内部结构。收益是新增玩法**零迁移成本**。

### 7.2 `event_config` 的字段分组

`schema` 共 26 个字段，按用途分四组（`event-config.js`）：

| 分组 | 字段 |
|---|---|
| **硬件与记录** | `supports_smart_cube`、`record_steps`、`record_tps`、`record_video`、`record_algorithm` |
| **尝试编号** | `attempt_id_type`(number/letter/custom)、`attempt_id_label`、`max_attempts`、`best_of` |
| **判定规则** | `penalty_types`、`allow_plus_two`、`allow_dnf`、`use_wca_rules`、`timer_precision`、`inspection_time`、`cumulative_time` |
| **显示设置** | `sort_order`、`show_in_leaderboard`、`is_official_wca` |
| **算法（也被 `algorithm_config` 复用）** | `algorithm_type`、`window_size`、`trim_count`、`is_lower_better`、`threshold`、`inherit_from_parent` |

每个字段都是自描述的 `{type, default, label, description}`，前端据此**自动生成表单**——加一个字段不需要改 HTML，改 `schema` 就够了。这就是 `renderEventConfigForm()` 能通吃所有项目的原因。

### 7.3 五种算法

| `algorithm_type` | 含义 | 用到的参数 |
|---|---|---|
| `single` | 单次最好 | — |
| `average` | 去头尾平均（WCA 标准） | `window_size`、`trim_count` |
| `mean` | 算术平均 | `window_size` |
| `best_of` | 取最好 N 次 | `window_size` |
| `sub` | 低于阈值的比例（输出百分比） | `threshold` |

`average` 是 WCA 的核心玩法：取最近 `window_size` 次，去掉 `trim_count` 个最高和 `trim_count` 个最低，剩下的求平均。AO5 就是 `window_size=5, trim_count=1`。

数据库侧有一份等价的 SQL 实现 `calculate_statistic()`，与前端 `renderStatsSummary()` 口径对齐（两者都只统计 `status='approved'` 且非 DNF/DNS/+2 的成绩）。

> **现状**：这个数据库函数**当前前端并未调用**，统计是在浏览器算的。保留它有两个用途：一是作为算法语义的权威参考实现，二是将来数据量大了可以下推到库端做聚合。同样的还有 `get_event_full_config()`。

### 7.4 21 个内置模板

`templates` 提供了 21 个常见玩法的完整预设，选中即填充表单：

| 类别 | 模板 |
|---|---|
| 正阶单次 | `3x3_single`、`2x2_single`、`4x4_single`、`5x5_single`、`6x6_single`、`7x7_single` |
| 三阶平均 | `3x3_ao5`、`3x3_ao12`、`3x3_ao50`、`3x3_ao100` |
| 单手 | `3x3_oh_single`、`3x3_oh_ao5` |
| 盲拧 | `3x3_bld`、`4x4_bld`、`5x5_bld`、`3x3_multibld` |
| 特殊 | `3x3_fmc`、`3x3_with_feet`、`3x3_relay` |
| 统计型 | `3x3_sub10`、`3x3_sub15` |

模板是**配置的起点而不是终点**：选完模板后仍可逐字段改，也可以直接编辑原始 JSON（`<details>` 里的高级面板），还能导出/导入 JSON 做批量搬运。

### 7.5 父子项目继承

设计意图：`3x3`（父）定义"支持智能魔方、记录步数、WCA 规则"，它的子项 `3x3-ao5`、`3x3-oh` 只写自己**不同**的那部分，其余自动继承。

实现方式在 `get_event_full_config()`：用递归 CTE 沿 `parent_event_id` 向上收集整条链，按深度**从远到近**合并 JSONB（`ORDER BY depth DESC` + `jsonb_object_agg`），后写的覆盖先写的——也就是**离自己越近的配置优先级越高**。

配置项 `inherit_from_parent` 就是这个机制的开关。

> **现状与缺口**：`get_event_full_config()` 与 `inherit_from_parent` 字段都已实现，但**前端还没有接上**——`loadEventConfigToForm()` 读的是项目自己的 `event_config`，不会向上合并。也就是说：**目前配置继承是"设计好了、DB 侧能跑、前端未启用"**。这是第 11 章待办里的第一条。

### 7.6 另一个已知缺口

`event_config` 里声明了 `supports_smart_cube` / `record_steps` / `record_tps` / `record_video`，本意是**驱动成绩录入表单**——某项目不支持智能魔方，就不该显示"智能魔方"选项。

但目前录入表单的字段显隐只看用户选的魔方类型（`toggleSmartFields()`），**没有读项目配置**。所以现在的实际行为是：配置能存能改能看，但还没管住录入界面。这也是待办项。

---

## 8. 前端设计

### 8.1 分层与加载顺序

无框架、无构建、无模块打包。所有 JS 通过 `<script>` 顺序加载，靠**全局函数**互相调用。顺序有严格依赖，不能随意调整：

| 顺序 | 文件 | 职责 | 行数 |
|:--:|---|---|---:|
| 1 | `event-config.js` | 项目配置 schema/模板（在 `<head>`，先于所有 js） | 966 |
| 2 | `js/config.js` | `WORKERS_BASE_URL`、`callWorker()`、`authToken()` | 67 |
| 3 | `js/session.js` | token 持久化与刷新 | 73 |
| 4 | `js/api.js` | `db()` 链式构建器、`apiFetch()` | 122 |
| 5 | `js/supabase-client.js` | `bootApp()` 启动序列（**文件名已名不副实**，见 §11） | 31 |
| 6 | `js/auth.js` | 角色常量、登录登出、`applyRoleUI()` | 176 |
| 7 | `js/ui-helpers.js` | `escHtml`、`showAlert`、`switchTab`、`guard` | 152 |
| 8 | `js/data-loader.js` | 全部 `load*` 读取函数 + Tabulator 实例 | 402 |
| 9 | `js/data-saver.js` | 全部 `add*` 写入函数 | 194 |
| 10 | `js/event-config-ui.js` | 配置表单渲染、模板、导入导出 | 238 |
| 11 | `js/admin.js` | 审核队列、用户管理 | 159 |
| 12 | `js/profile.js` | 我的档案、改昵称、改密码 | 143 |
| — | `js/stats.js` | **已废弃**，只剩说明注释，未被 `index.html` 引用 | 5 |

依赖方向是单向的：越靠后越"业务"，越靠前越"基础设施"。`config → session → api` 是最底层的三层，上层只依赖它们。

### 8.2 启动时序

`index.html` 末尾只做一件事：

```js
bootApp().catch(function (err) { /* 兜底提示 */ });
```

`bootApp()` 三步：

```
① 探测后端   db('competitions').select('id').limit(1)
             失败 → 提示"无法连接后端服务"并 return（不继续，避免满屏报错）
② 恢复会话   initAuth() → 读本地 token → setAuthUser() → 读 profiles.role
③ 加载数据   loadAllData() → 9 个 load* 并行（Promise.all）
```

在这里加 `.catch()` 是必要的：以前启动阶段的异常没人接管，只会静默失败——控制台报错、界面一片空白、用户完全不知道发生了什么。

### 8.3 `db()` 链式构建器

`js/api.js` 提供了一套语法**刻意对齐 supabase-js** 的构建器：

```js
// 迁移前（直连 supabase-js）
supabaseClient.from('competitions').select('*').order('competition_number')

// 迁移后（经云函数）
db('competitions').select('*').order('competition_number')
```

对齐语法不是为了好看，是为了让从直连架构迁移到云函数架构时，**业务代码只需机械替换前缀**。`events` 那个嵌套查询是直接受益者：

```js
db('attempts')
  .select('*, participants(name), competition_events(competitions(name), events(event_name))')
```

这套嵌套语法原样透传给 PostgREST（`?select=*,participants(name),...`），云函数不做任何解析——**它是代理而不是翻译器**，这是保持它能覆盖全部业务查询的关键。

实现上还有一个巧思：构建器对象上挂了 `then`，所以能直接 `await`：

```js
var { data, error } = await db('competitions').select('*').order('x');
```

`.single()` / `.maybeSingle()` 是老 API 里"取单条"的语义，这里用"取数组首项"近似实现，保持调用点不用改。

### 8.4 全局状态

三处全局状态，都是刻意保留的简单方案（没有状态管理库）：

| 变量 | 位置 | 含义 |
|---|---|---|
| `currentUser` / `currentProfile` / `currentRole` | `auth.js` | 当前登录者、其 profile、其角色 |
| `currentTab` | `supabase-client.js` | 当前标签页 |
| `competitionsTable` / `eventsTable` / … | `data-loader.js`、`admin.js`、`profile.js` | 8 个 Tabulator 实例 |

**表格实例必须显式声明并初始化为 `null`**。所有 `load*` 函数开头都是 `if (xTable) xTable.destroy()`——这是"先读取、后赋值"，如果变量不存在会直接抛 `ReferenceError`。`viewTable is not defined` 这个线上报错就是这么来的（当时 5 个变量全程是隐式全局，而 `viewTable` 的首次出现就是读取）。

### 8.5 三条全项目通行的约定

**① 所有动态数据进 `innerHTML` 前必须过 `escHtml()`**

```js
function escHtml(str) {
    return String(str == null ? '' : str)
        .replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;')
        .replace(/"/g,'&quot;').replace(/'/g,'&#39;');
}
```

这不是洁癖——**选手名、比赛名是编辑员可写的**，不转义就是存储型 XSS（见 §4.4）。用 `textContent` 的地方天然安全，问题只出在 `innerHTML` 拼接。

**② 写操作按钮统一走 `guard()`**

```html
<button onclick="guard(this, addCompetition)">➕ 添加比赛</button>
```

`guard()` 做三件事：防重复点击（`dataset.busy` 标记）、给出"处理中…"反馈（按钮禁用 + 文案替换）、异常兜底（`catch` 后弹 toast 并恢复按钮）。**手滑连点两次提交两条成绩**这种问题，在这一层就解决了。

**③ 反馈统一走 toast，登录错误例外**

普通反馈用顶部 `showAlert()`（4 秒自动消失）。但**登录错误就地显示**在表单下方（`#auth-error`，带 `role="alert"`），并自动把焦点移回用户ID框——因为登录失败时用户的注意力就在表单上，弹到页面顶部反而容易看不见。

---

## 9. 界面与视觉

### 9.1 设计令牌

所有样式基于 `css/style.css` 顶部的一组 CSS 变量，分七类：字体、中性色、文字色、主色、语义色、间距、圆角、字号、阴影。

| 类别 | 关键值 |
|---|---|
| 主色 | `--c-primary: #5B57D1`（白字对比度 5.6:1，满足 AA） |
| 文字 | `--c-text: #1A1D23`(15.6:1)、`--c-text-2: #4C5563`(7.8:1)、`--c-text-3: #6E7683`(4.6:1) |
| 语义色 | success `#17803D` / danger `#C62828` / warn `#B45309` / info `#0B6E8F`，各配一档 `-soft` 浅底 |
| 间距 | `--sp-1..7` = 4/8/12/16/24/32/40px，**全部是 4 的倍数** |
| 字号 | `--fs-xs..xl` = 12/13/14/16/20/24px |

**文字色都标注了对比度**（写在注释里），这不是装饰——`--c-text-3` 的 4.6:1 是刚好过 AA 的下限，再浅一档就不合格了。

间距全部取 4 的倍数，是为了让任意两个间距都能对齐出视觉节奏，避免出现"这里 13px 那里 15px"的散乱感。

### 9.2 导航与角色显隐

导航按**职责**分成三组，不是按页面类型平铺：

```
数据维护 │ 比赛管理  项目管理  选手管理
成绩     │ 成绩录入  成绩查询
治理     │ 我的档案  审核管理  用户管理
```

分组标题（`数据维护` / `成绩` / `治理`）本身就是信息——它告诉使用者"这几个功能是一类的"。这也是窄屏时选择**折行**而不是收进"更多 ▾"菜单的原因：收进菜单会丢掉分组语义。

显隐机制极简：给元素加 `data-minrole="editor"`，`applyRoleUI()` 按当前角色的 rank 统一设置 `display`。

```js
document.querySelectorAll('[data-minrole]').forEach(function (el) {
    var need = ROLE_RANK[el.getAttribute('data-minrole')] || 0;
    el.style.display = (rank >= need) ? '' : 'none';
});
```

角色变化后还要处理一个边界：**如果用户当前正停在一个已经没权限看的标签页上**（比如被降权了），不能让他干瞪在一个空白页。`ensureVisibleTab()` 会找第一个可见标签并自动切过去。

### 9.3 表格

用 Tabulator 5.6.0 + materialize 主题，但主题**必须排在 `style.css` 之前**：

```html
<link href=".../tabulator_materialize.min.css" rel="stylesheet">
<link rel="stylesheet" href="css/style.css">
```

顺序反了会出问题。原因是两边的选择器**权重相同**（都是 2 个类），同权重下后加载的胜出。反过来的话，materialize 的 `padding: 15px` 会压过项目里的 `padding: 2px 8px`——而项目用的是紧凑行高（`rowHeight: 26` / `headerHeight: 28`），单元格内容（约 51px）比单元格自身（约 30px）还高，被 `overflow: hidden` **纵向裁掉一半文字**。这个 bug 曾经让所有表格的文字都只剩上半截。

项目侧还把选择器提到 3~4 个类——单元格用 `.tabulator .tabulator-row .tabulator-cell`（3 个类，压过主题的 2 个类），表头用 `.tabulator .tabulator-header .tabulator-col .tabulator-col-content`（4 个类，与主题同权重）——把度量稳定收回到项目的紧凑规格：

| 部位 | 项目设定 | 装入的行高 |
|---|---|---|
| 单元格 | `padding: 2px 8px` + `line-height: 20px` + 字号 `--fs-sm` | `rowHeight: 26` |
| 表头 | `padding: 3px 8px` + `line-height: 22px` | `headerHeight: 28`（严格相等） |

表内控件（`.role-select`）也同步收进行高。

另外两个修过的坑，都是"列定义写了但拿不到值"：

- 列定义**没有 `field` 时不能用 `cell.getValue()`**。管理员表格的「角色」列、档案页的「状态」列都少了 `field`，`getValue()` 恒为 `undefined`，渲染出 `undefined` 徽章。用 `cell.getRow().getData()` 的合成列（操作/备注/时间）是合法的。
- 切标签时会给面板 `pane.focus()`（无障碍考虑）。它命中全局 `:focus-visible` 的 2px 描边，而面板占满整个内容区——于是整个页面被描了个大框。已加 `.tab-pane:focus-visible { outline: none }`，键盘焦点提示仍由 `.tab` 承担。

### 9.4 响应式与无障碍

断点两个：`max-width: 1100px` 和 `max-width: 767px`，另有一份 `@media print`。

**1100px 这一点**做的事：布局从两列变一列；导航**允许折行**（`flex-wrap: wrap`，整组一起折）并取消横滚。原先这里是 `overflow-x: auto`，在 900px 窗口下 8 个标签放不下，横向滚动条会**压在标签行上**、右侧「审核管理」被切掉。实测数据：修复前 `scrollWidth 995 > clientWidth 828`，修复后无溢出、2 行、末标签右边界在视口内；1400px 桌面端两种写法都是单行，观感不变。

无障碍方面已落实的：

- 页头 `<h1>`，各面板 `<h2>`，标题层级连续；
- 所有输入框有 `<label>`，视觉上不需要时用 `.sr-only` 隐藏而不删；
- 密码显隐按钮有 `aria-label` 与 `aria-pressed` 状态同步；
- 登录错误区 `role="alert"`，toast 区 `role="status" aria-live="polite"`；
- 切标签后把焦点移到面板（`tabindex="-1"`），键盘/读屏用户不会丢失上下文。

---

## 10. 部署与运维

### 10.1 两条流水线，且**不对称**

这是本系统最容易踩坑的一点：

```
前端：改代码 → git push main → GitHub Pages 自动构建 → 约 1~10 分钟生效
                                          （静态资源带 max-age=600，缓存期内需硬刷新）

后端：改 cf-workers/worker.js
       → cp cf-workers/worker.js cf-pages/dist/_worker.js   ★ 必须手动同步
       → 打开 Cloudflare Dashboard，把 cf-pages/dist 文件夹重新拖拽上传
       → 生效
```

**push 到 GitHub 不会更新后端**。后端的 Pages 项目是拖拽创建的类型，没有 Git 集成，`cf-workers/` 和 `cf-pages/` 只是"源码在仓库里"，部署动作仍然只有手动上传这一条路。

推论：

- 改了 `worker.js` 但忘了同步 `cf-pages/dist/_worker.js` → 部署的是旧逻辑；
- 同步了但忘了重新上传 → 线上还是旧逻辑；
- 改了环境变量但没重新部署 → **不生效**（这是"改了没反应"最常见的原因）。

存在两个 `worker.js` 副本是不得已的（一个给 Workers 控制台粘贴，一个给 Pages 拖拽上传），但它们是**同一份代码**，内容除头部注释外完全一致。改后端逻辑请改 `cf-workers/worker.js`，然后同步——不要分别改两份。

### 10.2 环境变量

在 Cloudflare Pages 项目的 **Settings → Variables and Secrets** 配置，三个：

| 变量 | 用途 | 敏感 |
|---|---|---|
| `SUPABASE_URL` | PostgREST / GoTrue 的基址 | 否 |
| `SUPABASE_ANON_KEY` | 作为 `apikey` 头；权限由 RLS 兜底 | 否（可下发） |
| `SUPABASE_SERVICE_ROLE_KEY` | **仅用于 `/admin-create-user` 调 Auth Admin API** | **是，必须选 Secret 类型** |

关于 `SERVICE_ROLE_KEY` 的设计约束（回应 C2）：它绕过 RLS，泄露等于全库沦陷（实测在 service_role 上下文能执行 `DROP TABLE events CASCADE`）。所以它的使用被压缩到最小：

- 只存在于 Worker 服务端环境变量，**永不下发浏览器**；
- 只用于"创建 auth.users"这一个天然需要高权限的动作；
- **数据面操作一律用调用者的 JWT**，不用它。云函数里 `supabaseRest()`（用调用者 token）和 `supabaseAdminRest()`（用 service_role）是两个独立函数，用途边界写在注释里。

### 10.3 首位管理员（鸡生蛋问题）

全新部署的库没有任何管理员，而"建管理员"需要管理员权限。标准流程：

1. 在 Supabase 后台 → Authentication → Users → **Add user**，邮箱填 `U000001@cube.local`（或任意 `<code>@cube.local`），**务必勾选 Auto Confirm User**；
2. 触发器 `handle_new_user` 会自动建 `profiles` 行，默认角色 `user`；
3. 在 SQL Editor 手动提升：
   ```sql
   UPDATE profiles SET role = 'admin' WHERE user_code = 'U000001';
   ```
   若 `user_code` 与预期不符（触发器按序列生成），先 `UPDATE ... SET user_code='U000001'` 对齐登录 ID；
4. 用该 ID 登录，之后所有账号都可以在「用户管理」里创建。

RLS 里的 `has_no_admin()` 口子（§4.2）是这条流程的兜底：万一没走 SQL 提升，系统在"一个管理员都没有"时也允许自助提升。有了管理员后它自动失效。

### 10.4 故障速查

| 现象 | 最可能的原因 | 排查 |
|---|---|---|
| `Failed to fetch` | ① 后端域名被墙（`workers.dev`）② CORS 预检失败 | 看报错里附带的**目标地址**（`config.js` 特意抛出来的）；`curl -X OPTIONS <url>/api/auth/login` 看是否返回 204 且带 `Access-Control-Allow-Origin` |
| `登录失败：用户ID或密码不正确` | 这是**好消息**——请求已打到 Supabase，是凭据不对 | 确认填的是用户ID（如 `u00000001`）不是昵称；确认账号已 Auto Confirm |
| 改了代码页面没变 | 缓存 | `Ctrl+Shift+R` 硬刷新；GitHub Pages 静态资源有 10 分钟缓存 |
| 改了 Worker 没生效 | 没重新部署，或环境变量改动后没重新部署 | 重新上传 `cf-pages/dist` |
| 表格文字只剩上半截 | 主题与 `style.css` 的加载顺序反了 | 主题 `<link>` 必须在 `style.css` 之前 |
| 某列显示 `undefined` | 列定义缺 `field` 却调了 `cell.getValue()` | 补 `field`，或改用 `getRow().getData()` |
| `X is not defined` | 表格实例变量没声明 | 在 `data-loader.js` 顶部显式声明为 `null` |

---

## 11. 已知限制与待办

按"该不该做"排序，不是按难度。

### 应当接上（设计已就绪，前端未启用）

1. **项目配置继承未接入**。`get_event_full_config()` 与 `inherit_from_parent` 都已实现，但 `loadEventConfigToForm()` 不向上合并父项目配置。当前子项目必须重复填写父项目已有的配置项。
2. **`event_config` 未驱动录入表单**。`supports_smart_cube` / `record_steps` / `record_tps` / `record_video` 存了但没用，录入界面仍只看用户选的魔方类型。配置改了不会影响录入表单。

### 命名与结构债（影响可读性，不影响运行）

3. **`js/supabase-client.js` 名不副实**。文件名叫 supabase client，内容其实是 `bootApp()` 启动序列。应改名为 `boot.js`，并同步 `index.html` 的引用。
4. **`js/stats.js` 是空壳**。只剩 5 行说明注释，且未被 `index.html` 引用。要么删除，要么在 README 里说明保留原因。
5. **统计有两份实现**。前端 `renderStatsSummary()`（实际使用）与数据库 `calculate_statistic()`（未被调用）。口径一致但没有自动化测试保证长期同步——改了一边忘了另一边，两边会悄悄产生分歧。
6. **两个 `worker.js` 副本靠人工同步**。这是"单文件可粘贴"与"Pages 拖拽部署"两个需求夹出来的结果，没有 CI 校验。可以考虑加一个 push 前的检查脚本比对两者。

### 有意的技术权衡（不建议改，但需知晓）

7. **`role` 是 PostgreSQL 保留字做列名**。真实 Supabase（PG15/16）可用，但属脆弱写法。更名为 `user_role` 会牵动云函数、前端、RLS 策略多处，收益低风险高，故保留。
8. **`sub` 算法的 `threshold` 无上界**。填 99999 会返回 100%，属统计口径问题而非安全漏洞。
9. **CORS 用 `Access-Control-Allow-Origin: *`**。这是安全的——本系统认证走 `Authorization` 头而不是 Cookie，不存在"浏览器自动携带凭据"的场景，所以通配符不会造成凭据泄露或 CSRF。
10. **`profiles` 表对匿名不可读**（策略是 `TO authenticated`）。匿名用户看不到任何用户信息，这是有意的——但意味着"匿名能看到选手名，看不到账号名"，两者是不同表，别混淆。
11. **无自动化前端测试**。验证手段是"无头 Edge 渲染 + 读布局数值"的临时脚本（`.workbuddy/` 下，不入库），以及 PGlite 的数据库层攻击测试（`.local-test/` 下，不入库）。

### 未来可能的方向

- **自定义域名**：绑自有域名可以彻底摆脱 `pages.dev`，是长期最稳的方案（自有域名一般不触发 SNI 封锁）。
- **数据量增长后的统计下推**：把 `renderStatsSummary()` 的聚合搬到 `calculate_statistic()`，前端只取结果。
- **成绩导入**：批量录入（CSV）会比逐条填表实用得多。

---

## 附录 A：文件清单

```
index.html              单页应用骨架（539 行）：身份条 + 三组导航 + 8 个标签面板
css/style.css           设计令牌与全部样式（687 行），7 个章节
event-config.js         项目配置 schema（26 字段）+ 21 个模板 + 校验（966 行）

js/config.js            后端地址、callWorker()、authToken()
js/session.js           token 持久化 / 刷新 / 清除
js/api.js               db() 链式构建器（语法对齐 supabase-js）
js/supabase-client.js   bootApp() 启动序列（★ 待改名）
js/auth.js              角色常量、登录登出、renderAuthUI / applyRoleUI
js/ui-helpers.js        escHtml / showAlert / switchTab / guard / 条件字段
js/data-loader.js       15 个 load* 读取函数 + 5 个表格实例
js/data-saver.js        5 个 add* 写入函数
js/event-config-ui.js   配置表单渲染 / 模板 / JSON 导入导出
js/admin.js             审核队列、用户管理（2 个表格实例）
js/profile.js           我的档案、改昵称、改密码
js/stats.js             已废弃空壳

cf-workers/worker.js            后端唯一源码（608 行，单文件无 import）
cf-workers/src/*.js             历史遗留的拆分版本，非部署产物
cf-pages/dist/_worker.js        部署产物（worker.js 副本 + 头部说明）
cf-pages/README.md              后端迁移与部署说明
schema.sql                      自包含建库脚本（7 个章节）
db-cleanup.sql                  废弃表清理

部署指南.md / 云函数部署说明.md / 权限系统说明.md / 升级说明.md / v4实现总结.md
                        历史文档（部分内容已过时，以本文档与代码为准）
```

## 附录 B：后端端点清单

| 端点 | 方法 | 鉴权 | 说明 |
|---|---|---|---|
| `/api/auth/login` | POST | 匿名 | `{code,password}` → access/refresh token，含大小写兜底 |
| `/api/auth/refresh` | POST | 匿名 | `{refresh_token}` → 新 access token |
| `/api/auth/logout` | POST | 匿名 | 使 refresh token 失效 |
| `/api/auth/password` | POST | Bearer | `{new_password}` 改密码 |
| `/api/auth/user` | GET | Bearer | 当前用户信息 |
| `/api/<table>` | GET | 匿名可读 | 通用数据代理 → PostgREST，RLS 生效 |
| `/api/<table>` | POST/PATCH/DELETE | **必须登录** | 写操作，以调用者 JWT 转发 |
| `/submit-attempt` | POST | 编辑员+ | 字段白名单 + 服务端定 status |
| `/review-attempt` | POST | 审核员+ | 防自审 + 乐观锁（409 冲突） |
| `/assign-role` | POST | 管理员 | 只能授低于管理员的角色 |
| `/admin-create-user` | POST | 管理员 | service_role 建 auth.users + 写 profile |
| `OPTIONS *` | OPTIONS | — | CORS 预检，返回 **204 且 body 为 null** |

> 预检返回 `new Response(null, {status:204})` 而非 `new Response("ok", {status:204})` —— 后者违反 fetch 规范（204 不得带响应体）会让 Workers 运行时抛 TypeError，表现为 1101/500 且**不带 CORS 头**，浏览器把所有请求报成 `Failed to fetch`。这个坑单体测试（curl 不预检）测不出来，必须用浏览器验证。

## 附录 C：数据库函数与触发器

**辅助函数**（均为 `STABLE SECURITY DEFINER` + 固定 `search_path`）：

| 函数 | 用途 |
|---|---|
| `get_my_role()` | 取当前用户角色 |
| `is_editor_or_above()` / `is_reviewer_or_above()` / `is_admin()` | 角色判断（RLS 策略里用） |
| `has_no_admin()` | 是否一个管理员都没有（部署引导口子） |

**业务函数**（`SECURITY INVOKER`，不绕过 RLS）：

| 函数 | 用途 | 前端是否调用 |
|---|---|---|
| `get_event_full_config(event_id)` | 递归合并父链配置 | 否（待接入） |
| `calculate_statistic(participant_id, event_id, algo_config)` | 按算法算成绩 | 否（保留参考实现） |

**触发器**：

| 触发器 | 时机 | 作用 |
|---|---|---|
| `on_auth_user_created` | `INSERT ON auth.users` | 自动建 `profiles` 行 |
| `trg_prevent_self_review` | `UPDATE OF status` | 禁止审核自己提交的成绩 |
| `trg_protect_review_columns` | `BEFORE UPDATE` | 非审核员不得改 `reviewed_by` / `reviewed_at` |
| `trg_backfill_submitted_by` | `BEFORE INSERT` | 自动补 `submitted_by = auth.uid()` |
| `trg_prevent_event_cycle` | `INSERT / UPDATE OF parent_event_id` | 拒绝父项目自环与间接成环（DoS 防护） |
