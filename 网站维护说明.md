# 网站维护说明

> 给运营委员会。看完这一篇就能改官网的绝大多数内容，不需要懂编程。

---

## 一、这个网站是什么

这是 **xssz 魔方非官方组织**的官网。它不是"成绩管理系统"——
成绩只是其中一页。对外它承担三件事：

1. **说清我们是谁**：组织定位、当前状态、架构与制度；
2. **晒出我们做了什么**：赛事记录与公开成绩；
3. **作为公信力凭证**：章程全文公开，学校要了解我们时可以直接看这个网址。

官网本身也是章程规定的职责：第十章「功能性机构」里的**运营委员会**，
职责含「线上社群与网站平台维护」。

---

## 二、文件都是干什么的

```
index.html      首页
about.html      关于我们（组织简介、状态说明、架构、公开范围）
charter.html    章程与制度（目录、检索、全文）
events.html     活动与赛事（读数据库）
results.html    赛事成绩（读数据库，任何人都能查）
login.html      成员登录
console.html    管理台（登录后可见）

css/style.css   全站样式。顶部是一组 CSS 变量（颜色/字号/间距），
                改这里能一次改全站，不要逐页改样式。

js/content.js   ★ 组织信息都在这里。改文案先看这个文件。
js/config.js    后端地址。⛔ 不要改，除非后端换了域名。
js/api.js       数据访问封装。
js/session.js   登录状态。
js/auth.js      登录/登出/角色显隐。
js/ui.js        转义、提示条、按钮守卫等公共工具。
js/site.js      导航高亮、移动端菜单、页脚、架构图渲染。
js/page-*.js    每个页面各自的逻辑，文件名对应页面。

data/charter/   ★ 章程与 4 份细则正文。见第三节第 3 条。
```

**基本规则**：改组织信息和文案 → 改 `js/content.js`；
改导航、加栏目、改页面标题 → 改各个 HTML。

---

## 三、常见任务

### 1. 改组织名称、简介、状态说明

打开 `js/content.js`，改引号里的字。存盘刷新即可。

文件里每个字段上方都有注释说明它是干什么的。
注意：字符串里要用双引号时，请用中文引号「」或 “”，不要用英文 `"`。

### 2. 改首页大标题下面那段话

这段为了"没装 JavaScript 也能读到"而在 HTML 里留了一份，
所以**要改两处**：`index.html` 的 hero 段落 + `js/content.js` 的 `ORG.lead`。

章程条数、章数同理（`index.html` 里的数字是兜底副本，
页面打开后会被 `content.js` 的值覆盖）。**章程一改，请优先更新 `content.js`。**

### 3. 更新章程或细则（最重要的一件事）

规范文件不是一份，是**五份**：主章程 + 4 份细则。每份都有通俗版与严谨版，
所以 `data/charter/` 下共 10 个 .md：

```
data/charter/plain/00-临时章程.md              主章程（通俗版）
data/charter/plain/01-特殊身份临时细则.md
data/charter/plain/02-历史管理委员会临时细则.md
data/charter/plain/03-运营委员会临时细则.md
data/charter/plain/04-事项分类细则.md
data/charter/strict/                            同上五份，严谨版
```

做法（约 1 分钟）：

1. 在章程仓库里导出：
   ```
   python 脚本/export_charter.py
   ```
2. 取对应的 Markdown，**整个文件内容**覆盖 `data/charter/<版本>/` 下的同名文件。
   文件名里的两位数字前缀决定网页上选择器的顺序，改名前先想清楚。
3. 提交推送。完成——章程页会自动解析出全部条文、目录、检索与交叉引用。

**新增或删除文件时**才需要动 `js/content.js` 的 `CHARTER.files` 数组：

```js
{ key: 'history', label: '历史管理委员会临时细则',
  file: '02-历史管理委员会临时细则.md', note: '史料的收集、记载与归档公开规则。' }
```

`key` 是 URL 参数（`charter.html?f=history`），`file` 是磁盘文件名，
`note` 是那张文件卡上的一句说明。三者都要有。

解析器认这几种行：

```
# 文件名                  ← 文档标题（h1）
## 第X章 章名              ← 章（h2）。细则可以整篇不分章
### / #### …              ← 节与子节（目前的文件里没有用到）
**第X条#哈希** 条文正文    ← 条文，哈希为 8 位十六进制
> 举例……                  ← 引用块。通俗版里的「举个例子」都是这种
---                       ← 分隔线，只作视觉分隔，不产生内容
其余为普通正文段落，支持有序/无序列表、列表嵌套与 **行内加粗**
```

认不出的行会被当成普通正文收下，不会报错、也不会丢内容。
但**章标题必须以 `## 第X章` 开头、条文标题必须是 `**第X条#hash**` 这种加粗行**，
否则那一部分不会按条例排版。

⚠️ 两版中同一条规则的哈希必须是同一个（哈希以严谨版正文为基准计算）。
增删条文后跑一次离线检查，它会逐条核对：
```bash
node .workbuddy/test_charter.js
```

### 4. 新增一个栏目（比如章程第十六章说的"线上社群"）

要改 5 个地方，缺一个就会出现"有的页面有、有的页面没有"：

1. 新建 `community.html`（复制一个现有页面的骨架改标题和正文）
2. 在**全部 7 个** HTML 文件的导航里加一行：
   ```html
   <a class="nav-link" href="community.html" data-nav="community.html">线上社群</a>
   ```
3. 在 `js/site.js` 的 `markCurrentNav()` 无需改动（它按 `data-nav` 自动匹配）
4. 新建 `js/page-community.js`，在该页底部引入
5. 在 `js/content.js` 里加这个栏目的内容数据

> 为什么导航要写 7 遍？因为这样**在 JavaScript 挂掉、或搜索引擎爬虫不执行 JS 时，
> 导航依然可用**。代价就是加栏目要改多处，已如实写在这里。

### 5. 改配色或字号

只用改 `css/style.css` 开头的 `:root { ... }` 里的变量。
比如主色是 `--c-primary: #5B57D1`。

⚠️ 改颜色前请留意注释里写的对比度。`--c-text-3` 的 4.6:1 是刚好过
无障碍标准（AA）的下限，**再调浅就不合格了**。

---

## 四、不要做的事

| 别动 | 原因 |
|---|---|
| `js/config.js` 的 `WORKERS_BASE_URL` | 这是后端唯一入口。改错全站数据都取不到 |
| 在页面里写数据库地址或密钥 | 架构上浏览器不接触数据库，写进去等于把钥匙贴门上 |
| 把某个功能做成"只有前端隐藏" | 前端隐藏只是界面礼貌。权限真正的锁在数据库里 |
| 在 `innerHTML` 里直接拼用户填的内容 | 选手名、比赛名是编辑员可写的，不转义会造成 XSS。用 `escHtml()` |

---

## 五、本地预览

不要直接双击 HTML 文件——那样章程页读不到 `data/charter/` 下的 .md
（浏览器的跨域限制会拦住本地文件读取）。

在项目目录里开一个本地服务器：

```bash
python -m http.server 8765
```

然后浏览器打开 `http://127.0.0.1:8765/`。

---

## 六、发布

前端是静态站，托管在 GitHub Pages：

```bash
git add -A
git commit -m "更新官网内容"
git push
```

推送后约 1~10 分钟生效（静态资源有 10 分钟缓存，
看不到变化就先 `Ctrl + Shift + R` 硬刷新）。

**后端不在这个流程里**。后端是另一个部署（Cloudflare Pages 拖拽上传），
改后端要单独操作，见 `cf-pages/README.md`。改前端内容不需要动后端。

---

## 七、出问题时先看这里

| 现象 | 原因 | 怎么办 |
|---|---|---|
| 页面显示"无法连接后端服务" | 后端域名被墙或改了地址 | 看报错里附带的**目标地址**；一般是 `js/config.js` 被改过 |
| 成绩页一直"加载中" | 网络慢或后端在重启 | 等几秒刷新。若一直如此，看控制台报错 |
| 章程页说"正文未能读取" | `data/charter/<版本>/` 下的 .md 缺失或文件名不匹配 | 见第三节第 3 条 |
| 改了内容页面没变 | 浏览器缓存 | `Ctrl + Shift + R` 硬刷新 |
| 导航点了没反应 | 链接写错或文件没建 | 检查 `href` 指向的文件是否真的存在 |
| 数据全是"暂无数据" | 数据库里确实还没有数据 | 需要先在管理台录入（管理台界面正在重建） |

---

## 八、当前的未完成项

1. **管理台界面重建中**：成绩录入、审核、用户管理、基础数据维护的界面还没做。
   后端接口是好的，权限体系也是完整生效的，缺的只是前端界面。
   现在登录后能看到身份和「我提交的成绩」。
2. **章程正文未导入**：见第三节第 3 条。
3. **活动与赛事页只有数据库里的内容**：如果有不记录成绩的纯活动（比如社团结对、
   线上交流），目前没有地方放。将来要么加一张表，要么放在 `js/content.js` 里。
